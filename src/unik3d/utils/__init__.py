"""UniK3D utility package.

VLN imports concrete utility modules directly. Keep package initialization
minimal so optional benchmark and compiled KNN dependencies are not required
for encoder inference.
"""
