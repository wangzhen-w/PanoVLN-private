# 验证记录

本文记录实际运行检查与历史验证结果；`instruction/tests` 已按项目整理要求移除。

## 分开控制 Habitat 与 API 并发（2026-09-12）

五阶段、提示词、审核条件、渲染参数和 ERP 格式不变。默认改为 16 个 Habitat 进程，每个进程最多并发处理 6 条轨迹；渲染操作始终由模拟器所属主线程执行。每个进程处理完当前场景后再切换场景。检查脚本和输出位于 `/workspace/tmp/panovln_concurrency/`。

- 调度检查覆盖并发上限、Habitat 线程归属、场景复用、渲染异常传递和任务失败时释放等待线程。
- 从两个场景各选两条较短轨迹，比较串行与交错执行的回放、分段渲染、上游重新渲染及 ERP 导出。pose、地面路线、测地距离和 162 张 ERP 完全一致。569 个媒体文件中 567 个字节一致；两张重新渲染的 compass 有极小像素差异，单通道最大差值 4/255，平均差值小于 0.001/255。视频与 depth 文件一致。
- 同四条轨迹经聚合器执行完整流程：2 条接受、2 条按现有局部审核隔离，0 条程序或 API 错误；39 次 API 调用、22 次缓存命中，导出 71 张 ERP。重跑全部复用，新增 API 调用为 0。这组短轨迹用于调度和格式检查，不作为质量或全量入库率估计。
- 在 GPU 0–7 上启动 16 个真实模拟器，完成透视和 ERP 观察后，同时发送 96 个原生视频请求。实测聚合器在途峰值 96，两个上游各 48，96 个请求全部成功。测试场景中每张卡的两个模拟器合计增加 1,362 MiB 显存，没有 OOM；这一显存测量不代表所有场景的峰值，短回复请求也不用于估计全量制作速度。

检查点与 ERP 目录随后统一为最终 R2R 的数字 `episode_id`，代码移除检查点哈希命名，不保留旧目录查找分支。一次性迁移 10,681 条检查点和 9,801 个图片目录，保留 9,801 条接受、832 条隔离和 48 条未完成状态；所有旧 R2R 内容保持相同，图片通过目录原地重命名保留。恢复检查确认已完成记录不调用渲染或 API。另按新目录规则重新导出上述四条轨迹的 162 张 ERP，与原图片逐字节一致。迁移备份位于生产工作目录的 `id_migration_backup/`，随最终成功导出清理。

真实全量任务恢复后，用仓库外的临时时间记录器观察 5 分钟，排除开头 1 分钟的媒体准备阶段，剩余约 240 秒内：16 个渲染进程平均忙碌 26.5%，API 平均在途 90.6 个（87–94），完成 1,247 个 API 请求，约 5.20 次/秒，客户端请求平均 17.47 秒。同期本地四个 vLLM 后端的平均请求耗时 16.50 秒，其中队列等待 0.34 秒、解码 14.50 秒。稳定阶段完成 67 条新轨迹。当前吞吐主要受模型推理限制，因此保留 16×6，不增加到 32 个模拟器。采样文件在 `/workspace/tmp/panovln_concurrency/profile16/samples/`；没有把诊断代码加入生产模块，也未改变生成规则。


## 简化局部审核与方向提示（2026-09-11）

保留原五阶段。生成端补充视频帧之间由 pose 计算的左右变化；审核端每个 segment 共享一次不含 instruction 的 clean 观察，再一次性比较完整局部文字，分别返回决策、运动和停止结论。移除了按决策重复制作的审核视频及截取普通运动句子后的独立请求。没有增加场景特例、逐词动作匹配或新的质量阈值。微小调整、近似地标、自然弯行和粗略停止区域仍可接受。

在之前随机审查的 8 条上完整试跑，用于开发回归；随后从排除全部 551 个已有生产检查点的 104,756 条轨迹中，以 `random.Random(20260912)` 均匀抽取 8 条，覆盖 7 个场景，完整生成、修复和导出。后一组 **8 条自动接受、0 条隔离或错误；5 条首次通过，3 条经局部修复通过**，生成 903 张 ERP，使用 99 次推理及 23 次缓存命中。最终实现的代码摘要与该次完整试跑一致。

Codex 按顺序查看后一组全部 segment 的 clean 顺序图，核对主要转向和终点。**7 条基本可导航，1 条仍有方向误判**；这是模拟人工复核，样本很小，不作为全量准确率估计。`trajectory_9aad90872800b98c` 的 s004 在灰色门入口实际左转，独立观察错误地读成右转，修复随后受到错误反馈影响。该问题没有因自动通过而被算作正确，也未向生产检查点写入人工改判。

方向提示确实帮助发现了一些错误：`trajectory_3123e03a19af951d` 初稿在下楼处写了右转，实际为左转，局部审核拦住后自动改正；原抽检样本 `trajectory_00d5b554f5abb525` 的楼梯顶部左右错误，在保留当前任务定义的对照中被拒绝，重新生成的局部语言方向正确。额外试验过让观察模型返回逐步骤帧区间，但没有稳定改善视觉定位，并会引入新的入口/转弯归属错误，已从最终代码移除。验收不追求每条文案完美，不为单个失败样本继续扩展规则。

同原来 8 条开发样本的 24 个 segment 对比，不计缓存、重试和修复时，一次完整审核的请求预算从 66 次减到 48 次，其中含视频的请求从 49 次减到 24 次。没有降低分辨率、核心视频覆盖或训练 ERP 质量；未做 48 进程全量吞吐对照，因此不据此承诺墙钟时间按同样比例下降。

- 最终 37 项 instruction 测试中，普通环境 36 项通过、原生解码项跳过；在 vLLM 环境补跑的 2 项视频测试均通过。
- 两组共 16 条、1,502 张 ERP 全部完整解码；JSON 与 gzip 对象一致，全部通过仓库原生 `VLNDatasetV1.from_json`，文本与接受记录一致，目标及 reference path 末点对应真实停止位置。
- 用正常 `generate` 入口恢复最终实现的 8 条已完成样本，全部复用，新增推理请求为 0。另在生产锁内确认原 341 条接受结果与 152 条隔离结果均可直接复用，不调用渲染器或模型。
- 生产任务保持暂停。按用户要求保留已有 341 条接受结果、152 条隔离结果以及 31,365 张 ERP，原始轨迹文件 SHA-256 未变。完成结果保留原生成策略来源；这不表示旧数据经过了新审核。58 条未完成或报错记录保留草稿和媒体，恢复时使用当前审核。检查点迁移先备份，再更新兼容签名，备份位于 `PanoVLN/.work/instruction/continuation_backup/`，随最终成功导出清理。

本次样本、对照、模拟复核和检查记录保存在 `/workspace/tmp/panovln_instruction_simplify/`。其中 `holdout/` 为最终实现的完整试跑，`recheck/` 是未采用的逐步骤定位试验，不属于当前实现的质量结果。

## 决策审核对照与复审（2026-09-11）

全量暂停时已有 252 条完成：103 条接受、149 条隔离，另有 48 条进行中。149 条隔离包含 67 条上游材料失败（66 条候选不可见、1 条 anchor 与路线距离异常）、79 条局部验证失败和 3 条输出契约失败。因此不能把隔离率当作 instruction 错误率。

对照固定原 instruction、轨迹、pose 和模型，重新制作旧实现 `8c41427` 的候选题。定向选择 8 个被拒绝的局部决策及 4 个已接受的对照；这不是独立随机评测集。Codex 查看路线顺序画面、compass，并在有方向争议时核对真实姿态。8 个拒绝中，7 个局部描述足以导航，1 个实际写反方向；4 个接受对照中另有 1 个写反方向。判断针对选中的局部决策，不代表整条 instruction 均正确。

例如 `trajectory_0050218a95868f3a` 的“Turn right at the staircase and climb the steps.”与真实右转上楼一致，但程序把正确字母 A 标在前门地毯附近，Qwen 把靠近楼梯的 C 当成正确入口。将标记从 connection 中心移到分支地面后，固定视点下能显示全部标记的 7 题均答对，但另 5 题无法显示全部地面标记；因此仅移动字母仍不能作为通用解决办法。抽象导航分区边界也不一定与人描述的转向时刻重合。

宽泛地让模型同时看视频与待审文字，会出现顺着文字解释画面的错误。最终采用两步局部审核：先不给 instruction，独立观察 clean 决策区域；再检查文字是否与观察到的路径一致、是否能区分入口。这里“独立”指观察输入不含待审文字，仍使用同一 Qwen 模型，并非独立人工真值。

固定文本对照均使用 temperature=0；生产仍使用配置中的 0.15。最终任务定义的对照结果：

| 对照内容 | 结果 |
| --- | ---: |
| 12 个原始局部描述中的 10 个可用描述 | 10 个通过 |
| 2 个真实的左右错误 | 2 个拒绝 |
| 将这 2 个错误改为实际方向 | 2 个通过 |
| 6 个故意改错的方向、入口或上下楼描述 | 6 个拒绝 |
| 2 个无法区分多个入口的泛化描述 | 2 个信息不足 |

这些 22 项用于问题排查和任务定义回归，不能作为全量正确率估计。中间试验未采用的标记、提示词和 thinking 配置没有加入生产分支。

随后对 24 条、23 个场景执行完整生成/局部修复/导出流程：复用已有局部草稿重新审核，其中 6 条草稿在本次排查中新生成；不是 24 条完全从零的独立采样。GPU 4–7、4 个进程，最终 **21 条接受、3 条隔离，0 条错误或未完成**。这组样本原状态为 4 条接受、14 条隔离、6 条未完成；其中原来隔离的 **13 / 14 条恢复入库**。这是问题样本复审，不可外推全量通过率。复审阶段 8 条无需再次改写，13 条经局部修复通过。

- 21 条接受结果导出 2,222 张 RGB、1600×800 clean ERP；每张 JPEG 完整解码，帧数与源动作数一致。
- JSON 与 gzip 解压对象相同，全部通过仓库原生 `VLNDatasetV1.from_json`；导出文本与完整通过的决策、普通运动、停止检查绑定一致。
- 训练图片目录仅包含接受的轨迹。观察请求不含 instruction、GT branch 或标记图；文字修复复用同一观察请求。
- 仍隔离的 `trajectory_00896b476b74c895`、`trajectory_0148c41ee8fb1473` 涉及普通运动审核，`trajectory_01134c89767aadba` 涉及另一决策 d1 的视觉观察与文字不一致。自动审核仍有误差，不能据此认定三条都是坏轨迹；未人工改判入库。
- 删除了把普通名词 `frame` 一律当作模型标记的限制，`door frame` 等场景表达可正常生成；明显的路线标记依赖仍受约束。
- 36 项 instruction 回归测试中，普通环境 35 项通过，原生视频解码项在 vLLM 环境单独通过。覆盖观察与待审文字隔离、完整审核区间、路径/入口双条件、失败后延后后续请求、空运动检查、clause/R2R/ERP 契约和中断续跑。

性能方面，instruction 原本就不运行 Habitat-Lab measures。取消重复场景分区、先修复首个失败片段、跳过空运动请求，均不减少渲染分辨率或帧覆盖。场景 `trajectory_0016303caa2cf3e1` 的旧/新实现交替回放实测：所有状态及起点/决策/STOP 的 RGB、depth、ERP 数组完全相同；热加载加回放约 2.19 s / 2.09 s，主要瓶颈仍是模型。独立观察的定向实验平均约 9.5 s，文字比较约 5.4 s；首次审核有额外调用，修复时可缓存复用，不据此承诺全量净提速。

排查材料、固定文本对照及验证报告保存在 `/workspace/tmp/panovln_decision_audit/`；完整复审产物在 `/workspace/tmp/panovln_instruction_review_final/`。生产轨迹文件没有改动。旧结果需要按新审核重新验证，不能仅修改代码指纹后直接沿用旧接受/隔离结论。

生产检查点已在任务锁内迁移：复用这次真实验证通过的 21 条及其 ERP，其他 279 条旧记录保留局部草稿、重置审核状态重新检查。原 300 条记录及旧 ERP 暂存在 `PanoVLN/.work/instruction/previous_run/`，随最终成功导出清理。没有将旧接受/隔离结论直接改签为新算法结果。已按原脚本配置在 `tmux data` 恢复全量，GPU 0–7、48 个进程；最终输出仍为 `PanoVLN/image/` 与根目录的 `train.json`、`train.json.gz`。

## 以下为旧选择题流程的历史验证

2026-09-11；Habitat-Sim 0.3.3，Qwen3.8-27B / vLLM，4 个 Habitat 工作进程。本轮采用自动生成、整理和局部修复，没有人工替换 instruction 或修改验证结论。

## 新场景抽样

从原 PanoVLN val 的 100 个场景中排除此前开发样本所在的 5 个场景，以 Python `random.Random(20260911)` 从排序后的剩余场景中抽取 24 个，再从每个场景抽取一条轨迹。原始输入现备份于 `PanoVLN_previous_layout/val/val.json.gz`；生产数据已统一，不再划分 train/val。24 条轨迹共 1,873 个动作、40 个决策点；其中 14 条含一个决策，6 条含两个，2 条含三个，2 条含四个。

| 结果 | 数量 |
| --- | ---: |
| 首次生成后直接通过 | 14 |
| 流水线局部修复后通过 | 6 |
| 自动入库合计 | **20 / 24（83.3%）** |
| 制作候选视觉材料时隔离 | 3 |
| 局部验证未通过而隔离 | 1 |
| API/程序错误、未完成 | 0 |

20 条入库轨迹包含 31 个决策点，导出 1,540 张 clean ERP。生成与验证共 233 次成功推理请求，36 次缓存命中；服务报告 697,950 个输入 token、27,435 个输出 token。

下表列出全部抽样结果，便于按 trajectory ID 重现选择。`修复` 为各 segment 的文字修复次数之和；这批未触发上游重制。

| trajectory_id | 场景 | 决策数 | 自动结果 | 修复 |
| --- | --- | ---: | --- | ---: |
| trajectory_cc726df3ac63d0ff | 00806-tQ5s4ShP627 | 1 | 通过 | 0 |
| trajectory_4a8ccc90200bc727 | 00816-q3hn1WQ12rz | 1 | 通过 | 0 |
| trajectory_b512f0df9d07bbd5 | 00820-mL8ThkuaVTM | 1 | 通过 | 1 |
| trajectory_1fb7f22567275a8d | 00822-nrA1tAA17Yp | 1 | 验证隔离 | 2 |
| trajectory_157b7b817297af89 | 00828-X7gTkoDHViv | 1 | 通过 | 1 |
| trajectory_fdd6aa72d29976bf | 00838-kJJyRFXVpx2 | 1 | 通过 | 1 |
| trajectory_4bf971480f518b31 | 00840-z9YwN9M8FpG | 2 | 通过 | 0 |
| trajectory_f780c327327ac4b8 | 00846-LNg5mXe1BDj | 2 | 通过 | 3 |
| trajectory_6b32ce66c8783be7 | 00847-bCPU9suPUw9 | 2 | 材料隔离 | 0 |
| trajectory_2103c3bb988420b4 | 00852-T6nG3E2Uui9 | 1 | 通过 | 0 |
| trajectory_bc96b13d18c2a5e1 | 00854-SiKqEZx7Ejt | 1 | 通过 | 1 |
| trajectory_3389e5096d208649 | 00858-cYkrGrCg2kB | 1 | 通过 | 0 |
| trajectory_061a91028809eabd | 00861-GLAQ4DNUx5U | 4 | 材料隔离 | 0 |
| trajectory_b722e6b98ed25a43 | 00863-b28CWbpQvor | 3 | 通过 | 0 |
| trajectory_bd21178d7d0c8ea5 | 00868-vd3HHTEpmyA | 1 | 通过 | 0 |
| trajectory_975f9f55b5558da4 | 00871-VBzV5z6i1WS | 4 | 通过 | 0 |
| trajectory_6bb9c04148eb2bf8 | 00875-66seV3BWPoX | 1 | 通过 | 0 |
| trajectory_a4cafbc04d90d650 | 00879-7Ukhou1GxYi | 2 | 材料隔离 | 0 |
| trajectory_739ce00251311cdb | 00884-XMHNu9rRQ1y | 2 | 通过 | 0 |
| trajectory_5ea8c0de13c9e8f6 | 00889-HMkoS756sz6 | 1 | 通过 | 0 |
| trajectory_6897ad050aea5f55 | 00891-cvZr5TUy5C5 | 3 | 通过 | 2 |
| trajectory_47470e3305403a8e | 00892-bzCsHPLDztK | 1 | 通过 | 0 |
| trajectory_230be3a323836c90 | 00895-auFeVz9Go4m | 2 | 通过 | 0 |
| trajectory_07026fb9627bfd72 | 00899-58NLZxWBSpk | 1 | 通过 | 0 |

## 视觉复核

Codex 查看了下面 8 条入库轨迹各段的 clean 顺序画面，并对关键位置补看 compass。按路径能否执行、关键入口是否明确、停止区域是否一致判断；没有逐帧要求文字，也没有改写样本文案。这是模拟人工复核，不是真实参与者实验或导航器成功率评测；复核样本按路径类型选取，不作为独立随机准确率估计。

| trajectory_id | 视觉判断 |
| --- | --- |
| trajectory_cc726df3ac63d0ff | 走廊直行进入客厅、到窗侧沙发区域的顺序清楚；停止描述略宽，可导航。 |
| trajectory_4a8ccc90200bc727 | 上楼和玻璃门所在房间的入口能定位；将终点房间称为 bedroom 较粗糙，但方向和玻璃门地标保留了识别依据。 |
| trajectory_b512f0df9d07bbd5 | 下楼、右侧入口、窗与餐桌之间的停止空间对应画面；下楼句子略重复，可保留。 |
| trajectory_fdd6aa72d29976bf | 厨房岛台与烤箱之间的通道、经过左侧餐桌及窗边角落都明确。 |
| trajectory_4bf971480f518b31 | 蓝色沙发旁下楼、进入餐厨区、白色冰箱附近停止，顺序和地标一致。 |
| trajectory_f780c327327ac4b8 | 厨房、下楼、躺椅和健身车的顺序清楚；终点白色板柜状物的名称略粗，但位置可识别。 |
| trajectory_b722e6b98ed25a43 | 三个选择依次对应厨房入口、微波炉旁右转和熨衣板房间，窗前停止一致。 |
| trajectory_975f9f55b5558da4 | 四个决策的连续入口、厨房通行和棕色扶手椅后的左侧卧室入口清楚，终点为床与办公椅前；大小写衔接可忽略。 |

另复核了验证隔离的 `trajectory_1fb7f22567275a8d`。最终文本为：

> Move forward through the doorway into the floral-walled hall. At the landing, turn left to descend the curved staircase. Stop on the stairs just before the landing with the dark rug and cowhide.

文字基本能描述画面中的进门、左转下楼和停止。自动决策检查把前面的进门句误当成当前要执行的选择，选择了 A；程序掌握的实际分支是 B。B 的标记靠近楼梯旁墙面，也增加了关联难度。普通运动检查还把已在独立决策 clause 中描述的下楼当作遗漏。它说明自动验证仍有误拒，不能把所有隔离都当成 instruction 本身不可用。本轮保留原自动结果，该条未人工改判入库。

## 三条材料隔离

- `trajectory_6b32ce66c8783be7`：d1 的多个选择前视点均无法显示候选标签，虽然地面路线可见。
- `trajectory_061a91028809eabd`：d2 在有效 decision region 内只能看到一个候选标签，无法构成可靠的入口比较。
- `trajectory_a4cafbc04d90d650`：d0 计算得到的选择前视点均在标注的 decision region 外，未进入语言生成。

这些是候选投影、可见性或视点与 region 对齐问题，不能仅凭上述诊断认定轨迹错误或场景扫描损坏。后续提高产率应先统一改进候选材料和进度对齐；不针对这几条轨迹添加特殊规则，也不直接降低入口正确性要求。这 24 条结果不能直接外推全量通过率。

## 工程检查

- 30 项独立契约测试通过。普通环境运行 29 项，原生 vLLM 视频解码项在 vLLM 环境单独通过。覆盖投影与遮挡、动作/决策/STOP 归属、答案隔离、局部修复、clause 来源、全帧视频传输、R2R 导出与训练图片生命周期。
- 4 个不同进程分别使用 GPU 4、5、6、7 完成实测。
- `.json` 与 `.json.gz` 解压后的对象完全相同；20 条均通过仓库原生 `VLNDatasetV1.from_json`，场景路径均存在。
- 每条导出文本与最终验证文本一致；目标及 reference path 末点为源轨迹的真实 stop。
- 1,540 张 JPEG 全部完整解码，为 RGB、1600×800。每个动作对应一张动作前观察帧，保留原地旋转与 STOP 观察，不含 STOP 后重复帧。
- 最终 ERP 目录仅包含 20 条入库轨迹，没有隔离样本的图片。输出总大小为 669,955,840 字节。
- 用正常 `generate` 入口恢复已完成记录，新增推理请求为 0；关闭 `KEEP_WORK` 后工作目录自动删除，约 903 MB 的制作材料与过程缓存被清理。

## 抽样输出归档

```text
/workspace/data2/dataset/general_VLN_data/PanoVLN_previous_layout/val/instruction/quality_check/
├── val.json
├── val.json.gz
└── erp/<trajectory_id>/frame_<action_index>.jpg
```

本轮复核完成后通过正常生成入口恢复已完成记录并清理工作目录。生产配置默认自动清理；仅调试时使用 `KEEP_WORK=true`。没有启动全量数据生成。

## 统一目录与中断续跑验证

同日将原两部分轨迹合并为 `PanoVLN/trajectory/trajectories.json.gz`，共 **105,307 条（94,745 + 10,562）**，覆盖 872 个有轨迹的场景；采集统计包含全部 900 个场景。完整核对所有源记录与合并记录，ID 无重复，动作、pose、决策事件、实际停止位置及其他 episode 字段逐条相同。合并统计中的全量回放结论继承自未改动的源轨迹，并明确标记为历史结果。

- 使用更新后的轨迹回放入口，从两个源资源目录各抽取首尾场景中的一条，共 4 条、4 个场景，全部回放成功。决策位置最大误差约 `5.6e-8 m`，终点位置最大误差约 `6.1e-8 m`。这次是抽样回放，不是重新全量回放。
- 轨迹 `inspect` 发现 900 个场景；已完成采集入口返回 `already_collected`，保留合并数据。Instruction `inspect` 选中全部 105,307 条，配置为 GPU 0–7、48 个进程、`LIMIT=0`。本次没有启动 48 进程全量生成或做峰值显存压测。
- 真实 Habitat/Qwen 中断试验使用 GPU 6、7 和 2 个进程。一条 `trajectory_cc726df3ac63d0ff` 完成、另一条 `trajectory_07026fb9627bfd72` 保存 6 段局部结果后，终止整个测试进程组，再以完全相同设置重启。完成样本直接复用且文字未变；另一条继续处理，恢复运行记录 5 次缓存命中、14 次新增推理，最终两条均通过。
- 恢复结果的 155 张 ERP 全部完整解码，均为 RGB、1600×800，帧数与各自源动作数一致；JSON 与 gzip 解压后的对象相同。关闭 `KEEP_WORK` 再跑，新增推理为 0，工作目录被清理；再次运行返回 `already_exported`，最终文本不变。
- 当前共 38 项独立测试通过：34 项 instruction 测试与 4 项统一采集/合并测试；其中原生视频解码项在 vLLM 环境单独运行。新增中断检查覆盖强制终止 ERP 写入、模型响应缓存复用、JSON/gzip 发布中断，以及最终检查点清理中断。

上述中断试验的临时媒体在检查完成后清理。生产输出为 `PanoVLN/image/`、`PanoVLN/train.json` 和 `PanoVLN/train.json.gz`；恢复检查点保存在 `PanoVLN/.work/instruction/`，完成后自动删除。中断时尚未保存的当前请求或写入可能重做。

## 总进度显示

新增主进程统一的 `tqdm` 进度条后，运行单进程、两个 `spawn` 工作进程、`render` 模式和工作进程异常检查。检查使用真实的工作进程调度与结果上报，模拟渲染/模型步骤；让后一条轨迹等待主进程收到前一条进度，确认进度会在整个分片结束前更新。总数、通过/隔离/错误/复用计数均正确，进程结束前队列中的剩余进度全部计入，异常能传回主进程并关闭进度条。重新运行 instruction 回归测试，33 项通过，原生视频解码项在当前环境跳过（该项此前已在 vLLM 环境通过）。

全量启动后，Habitat 的可选 semantic 描述加载诊断仍会越过 `quiet` 日志级别，因此对场景创建和关闭增加原生日志捕获，异常时重放诊断。使用 `trajectory_0067de6b5d630184` 实测：修复前后的 87 个回放状态、贴地路线采样完全一致，起点、中段与 STOP 位置的 RGB、深度和 ERP 数组逐元素相同，正常加载不再刷屏。独立检查确认原生 stdout/stderr 在成功时隐藏、失败时保留，异常和终端输出均正确恢复。该修改只涉及输出显示，已核对并迁移当前 74 条生产检查点的代码指纹，其他记录字段及缓存保持原样。

## 收尾调度与断点复用

2026-09-14，全量进度约 98% 时，固定场景分片的剩余工作集中在少数进程。12 个可用 vLLM 实例的 62.6 秒采样中，实际在途请求平均仅 10.3 个、范围 9–12 个，新增请求约 1.42 次/秒，没有新增上游错误。因此改为启动时统计已完成检查点，只将剩余轨迹按场景分成有限批次，由空闲工作进程继续领取。五阶段任务、提示词、渲染和审核条件保持不变。

- 使用 3 个真实 `spawn` 工作进程检查不均匀任务调度：12 条已完成记录直接复用，96 条新任务各执行一次；工作进程分别处理 32、24、40 条，空闲进程能够领取额外批次。模拟器调用均在所属主线程执行。
- 两个真实 Habitat 工作进程、4 条短轨迹完成生成和局部审核，3 条接受、1 条隔离，无程序或 API 错误；44 次新增请求、12 次缓存命中。这是调度功能检查，不是质量通过率估计。
- 118 张 clean ERP 全部完整解码，为 RGB、1600×800；JSON 与 gzip 解压对象相同。以正常入口再次运行，新增推理与渲染工作进程均为 0，检查点、ERP 和 JSON 字节保持不变。
- 临时迁移工具在接受、隔离、处理中三种检查点上检查了原始内容保留和重复执行；生产续跑使用显式指纹迁移与原始文件备份，不在正常 pipeline 中增加旧版本兼容分支。
- 生产迁移保留 102,909 份原始记录：95,828 条接受、7,066 条隔离、3 条错误、12 条处理中。逐文件核对仅续跑指纹变化，其余字节相同；源轨迹 SHA-256 保持 `cd96156132b13930333d4dab33082bed7c831a33068b761bb5b6cb883282b05f`，图片未改动。
- 剩余任务最多的场景在同一张 GPU 上运行两个模拟器，均完成 RGB、depth 和 1600×800 ERP 观察；本地 vLLM 常驻时，该卡显存由 72,962 MiB 增至 80,918 MiB。因此保留每卡两个模拟器；此检查不代表所有场景和模型负载的峰值显存上界。

生产脚本保持 16 个 Habitat 进程，将每进程工作流上限调整为 9，对应最多 144 个并发 API 工作流。该数字是并发上限，实际吞吐仍取决于渲染、片段数量、修复次数和模型服务负载。

恢复后的 181.9 秒连续采样中，平均 125.6 个请求在途、范围 80–140，新增 1,434 次请求（7.88 次/秒），完成 43 条轨迹（4.23 秒/条）。该窗口末尾 58.4 秒完成 17 条（3.44 秒/条）。这是当前剩余任务的短时实测，包含负载上升过程，不作为全量固定速度承诺。另一次重叠的 105.4 秒采样中，16 个渲染主线程平均使用约 29.6% 的单核 CPU；实际模型请求已接近工作流并发上限。

上述窗口远端上游增加 1 次错误与 1 次重试，本地上游无新增错误；12 个模型实例仍全部可用，生成进度未报新增 episode error，未出现 OOM。五阶段任务和审核条件未因提速调整。
